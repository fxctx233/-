# 本地记账与存款计划

状态：需求初稿，尚未实现可执行程序。

## 目标

先制作 Windows PC 版，再制作安卓版。数据只保存在设备本地，无需登录，不上传账目。支持通过文件在手机和电脑之间备份、恢复。

## 第一版功能

- 手动新增、编辑、删除收入和支出；可补记历史日期。删除前确认。
- 每笔记录包括日期、收支方向、分类、金额、备注。金额按人民币分存储，避免小数计算误差。
- 支出预置餐饮、工具、娱乐、交通、住房、购物、医疗、其他；收入预置工资、股票、公积金、奖金、其他。支持自定义分类。已使用分类不能直接删除，应归档或迁移记录。
- 按年、月、日统计收入、支出、结余；支持分类筛选、分类汇总以及查看对应明细。
- 股票收入默认手动记录已实现收益，不自动计算行情或总资产。公积金可记录，但不能默认视为可随时取用的现金。
- 多个存款计划：名称、目标金额、截止日期、初始已存金额、提醒设置。
- 计划有独立存入和取出记录，可修改；显示累计已存、剩余金额、完成比例、剩余时间。存入总额不得因编辑或取出变成负数。
- 显示为按期完成目标所需的平均每月存款额，并明确计算口径；过期未完成时显示逾期，不产生负数或除零计算。
- 记账结余不自动转为已存款。存款是资金分配，不重复计为支出；取出存款不重复计为收入。
- 应用内展示待办存款提醒；关闭应用后的定时提醒需要分别接入 Windows 和 Android 系统调度，不能仅靠应用页面计时器。

## 本地数据与备份

- 使用 SQLite 保存结构化账目及计划，不保存票据图片或附件，控制空间占用。
- 程序安装体积与账目数据体积分开评估；最终以实际构建和数据测试为准，不预先承诺安装包大小。
- 导出一个带格式版本号的完整备份文件，包含分类、账目、计划、存款记录及必要设置。
- 两端遵循同一备份格式。通过 USB、文件传输等方式自行将手机备份复制到电脑；也支持反向恢复。
- 第一版导入采用完整恢复，不自动合并两个设备的独立账本。导入前展示摘要、确认替换、自动备份现有数据。
- 导入先校验格式、版本、金额、日期、记录引用及唯一标识，再以数据库事务整体恢复；失败不能破坏现有账本。
- 数据存放在用户应用数据目录，不能放在临时目录。导出文件由用户选择位置。
- 本地保存不等于加密：若第一版使用明文备份，界面必须提示财务隐私风险，不能宣称已加密。
- 不默认提供实时跨设备同步；两台设备同时记账后不能通过覆盖恢复安全合并，界面必须说明。

## 页面

1. 总览：本月收入、支出、结余、存款目标进度与提醒。
2. 记一笔：方向、金额、日期、分类、备注。
3. 账单与统计：年月日切换、筛选、汇总、编辑和删除。
4. 存款计划：创建与编辑计划、存入与取出、明细、剩余金额。
5. 设置：分类管理、备份导出、恢复导入、提醒设置、数据路径说明。

## 开发顺序

1. 确定界面框架，建立共享数据与备份规范。
2. 完成 Windows 版的记账、统计、计划、导出与恢复。
3. 验证金额精度、跨年月统计、记录编辑、计划进度及异常备份恢复。
4. 制作安卓界面，接入本地通知和文件选择、分享。
5. 验证手机导出、电脑恢复以及电脑导出、手机恢复；分别测试关闭应用后的提醒。

## 交付验收

- 断网可完成记账、编辑、统计、计划和备份操作。
- 修改历史账目后，相应日、月、年与分类汇总同步更新。
- 重启软件后数据仍存在。
- 存款进度由存款记录计算，达到或超过目标后剩余金额为零。
- 损坏或不兼容的备份会明确报错且不修改现有数据。
- 手机与电脑可互相恢复同一个完整备份，不重复生成记录。
- 系统提醒被禁用时提供状态提示；不承诺关机状态下提醒。
